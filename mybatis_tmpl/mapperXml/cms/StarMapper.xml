<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.phh.dao.cms.StarDAO">

    <resultMap id="BaseResultMap" type="com.phh.do.cms.StarDO">
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="userId" jdbcType="BIGINT" property="userId"/>
        <result column="articleId" jdbcType="BIGINT" property="articleId"/>
        <result column="createdAt" jdbcType="DATETIME" property="createdAt"/>
        <result column="updatedAt" jdbcType="DATETIME" property="updatedAt"/>
        <result column="state" jdbcType="TINYINT" property="state"/>
    </resultMap>

    <sql id="Base_Column_List">
         id, userId, articleId, createdAt, updatedAt, state
    </sql>

    <select id="getById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM cms_star
        WHERE id = #{id,jdbcType=BIGINT}
    </select>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM cms_star WHERE id = #{id,jdbcType=BIGINT}
    </delete>

    <insert id="insert" parameterType="com.phh.do.cms.StarDO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO cms_star ( id, userId, articleId, createdAt, updatedAt, state,)
        VALUES (#{id,jdbcType=BIGINT}, #{userId,jdbcType=BIGINT}, #{articleId,jdbcType=BIGINT}, #{createdAt,jdbcType=DATETIME}, #{updatedAt,jdbcType=DATETIME}, #{state,jdbcType=TINYINT})
    </insert>

    <update id="update" parameterType="com.phh.do.cms.StarDO">
        UPDATE cms_star
        <set>
            <if test="userId != null">
                userId = #{userId,jdbcType=BIGINT},
            </if>
            <if test="articleId != null">
                articleId = #{articleId,jdbcType=BIGINT},
            </if>
            <if test="createdAt != null">
                createdAt = #{createdAt,jdbcType=DATETIME},
            </if>
            <if test="updatedAt != null">
                updatedAt = #{updatedAt,jdbcType=DATETIME},
            </if>
            <if test="state != null">
                state = #{state,jdbcType=TINYINT},
            </if>
        </set>
        WHERE id = #{id,jdbcType=BIGINT}
    </update>

</mapper>